# DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:mariadb://localhost:3306/torrex_fitness?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=password

# In-memory DATASOURCE
#spring.secondDatasource.url=jdbc:h2:mem:testdb
#spring.secondDatasource.driverClassName=org.h2.Driver
#spring.secondDatasource.username=root
#spring.secondDatasource.password=password
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# spring security credentials
# spring.security.user.name=root
# spring.security.user.password=password

# Hibernate
# The SQL dialect makes Hibernate generate better SQL for the chosen database
# MariaDB103Dialect is for MariaDB Server 10.3 and later
# My current MariaDB Server is version 10.7.2
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDB103Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=DEBUG

# allows you to see beans
management.endpoints.web.exposure.include=*
